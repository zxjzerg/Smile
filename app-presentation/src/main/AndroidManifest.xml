<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.zxjdev.smile"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".presentation.application.SmileApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Smile">

        <!-- authorization -->
        <activity android:name=".presentation.common.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentation.user.authorization.login.LoginActivity"
            android:parentActivityName=".presentation.common.splash.SplashActivity">
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".presentation.common.splash.SplashActivity" />
        </activity>

        <activity
            android:name=".presentation.user.authorization.register.RegisterActivity"
            android:parentActivityName=".presentation.common.splash.SplashActivity">
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".presentation.common.splash.SplashActivity" />
        </activity>

        <activity
            android:name=".presentation.common.main.MainActivity"
            android:launchMode="singleTask" />

        <activity
            android:name=".presentation.moment.create.NewMomentActivity"
            android:parentActivityName=".presentation.common.main.MainActivity">
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".presentation.common.main.MainActivity" />
        </activity>
    </application>

</manifest>